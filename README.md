для тестирования в RIDER добавить перменные в Run → Edit Configurations...
найди поле "Environment variables"

sudo apt install postgresql postgresql-contrib -y

pg_config --version
PostgreSQL 16.9 (Ubuntu 16.9-0ubuntu0.24.04.1)
